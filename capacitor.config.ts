import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Mental Health Mobile App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
